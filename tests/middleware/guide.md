# Middleware tests guide

This folder checks the app behavior that happens before and after routes run.

## File purposes

- `test_middleware.py` verifies security headers, CORS behavior, request IDs, database cleanup, and error responses.
- `guide.md` is this plain-language guide to middleware tests.
