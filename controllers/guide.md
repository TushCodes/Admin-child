# Controllers module guide

This folder contains shared website helpers that prepare route responses. Admin-specific controller helpers live under `admin/controllers/`.

## File purposes

- `__init__.py` gathers the public response helpers in one import place.
- `responses.py` creates consistent JSON success and error messages.
- `guide.md` is this plain-language guide to the controllers module.
