# Core JavaScript guide

This folder contains reusable browser utilities for multiple pages.

## File purposes

- `api-client.js` wraps browser `fetch` calls with common JSON and error handling.
- `storage.js` safely reads and writes browser storage values.
- `toast.js` shows short success or error messages to visitors.
- `guide.md` is this plain-language guide to core JavaScript.
